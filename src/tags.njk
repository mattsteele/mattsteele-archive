---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - home
    - work
layout: layouts/home.njk
renderData:
  title: Tagged “{{ tag }}”
  permalink: /tags/{{ tag }}/
excludeFromSitemap: true
---
<h1>Tagged “{{ tag }}”</h1>

{% set postslist = collections[ tag ] %}
{% include "postslist.njk" %}

{% set worklist = collections[ tag ] %}
{% include "worklist.njk" %}

<p>See <a href="{{ '/tags/' | url }}">all tags</a>.</p>
