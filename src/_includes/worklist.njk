<div class="note">
  <p><b>Note:</b> My work for the past few years has been in JavaScript-based application prototyping. Prototyping is, by its very nature, experimental; design ideas that are tested &amp; iterated upon. That work is proprietary &amp; cannot be shared publicly. Below is earlier work.</p>
</div><!--- .note -->
{% for work in worklist | reverse %}
  <section class="work-listing-item">
    <a href="{{ work.url | url }}">
      <h2 class="work-entry-title">{% if work.data.title %}{{ work.data.title }}{% else %}<code>{{ work.url }}</code>{% endif %}</h2>
      <img loading="lazy" src="/assets/images/work-images/{{ work.data.featured_image }}" alt="{{ work.data.title }}">
    </a>
    <p class="work-meta">Work done with <a href="{{ work.data.work_with_url }}" target="_blank" rel="noopener noreferrer">{{ work.data.work_with_title }}</a></p>
  </section>
{% endfor %}
